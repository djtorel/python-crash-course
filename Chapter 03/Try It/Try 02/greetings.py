# Start with the list from previous exercise, but instead of just printing
# each name, print a message to them. The text of each message should be
# the same.

names = ["Adam", "Mike", "Ryan", "Stephen"]
print("Hello " + names[0] + "!")
print("Hello " + names[1] + "!")
print("Hello " + names[2] + "!")
print("Hello " + names[3] + "!")
